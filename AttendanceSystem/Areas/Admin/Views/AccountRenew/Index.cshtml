@using AttendanceSystem.ViewModel;
@model List<AttendanceSystem.ViewModel.CompanyRenewPaymentVM>
@{
    /**/

    ViewBag.Title = "AccountRenew";
    int counter = 1;
}

<section class="section">
    <div class="section-body">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h4>Account Package Renew List</h4>
                    </div>
                    <div class="card-body">
                        <div class="pull-right">
                            <a href="/admin/accountrenew/buy" class="btn btn-icon icon-left btn-primary"><i class="fas fa-plus-circle"></i> Buy New Account Package</a>
                        </div>
                        <div class="table-responsive">
                            <table class="table table-striped table-hover" id="tableExport" style="width:100%;">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Renew Date</th>
                                        <th>Package Name</th>
                                        <th>Amount</th>
                                        <th>Payment For</th>
                                        <th>Start Date</th>
                                        <th>End Date</th>
                                        <th>Access Days</th>
                                        <th>No Of Employee</th>
                                        <th>No Of SMS</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @if (Model != null && Model.Count > 0)
                                    {
                                        foreach (CompanyRenewPaymentVM obj in Model)
                                        {
                                            <tr class="gradeX">
                                                <td>@counter.</td>
                                                <td>@obj.CreatedDate.ToString("dd/MM/yyyy hh:mm tt")</td>
                                                <td>@obj.PackageName</td>
                                                <td>@obj.Amount</td>
                                                <td>@obj.PaymentFor</td>
                                                <td>@obj.StartDate</td>
                                                <td>@obj.EndDate</td>
                                                <td>@obj.AccessDays</td>
                                                <td>@obj.NoOfEmployee</td>
                                                <td>@obj.NoOfSMS</td>
                                                <td>
                                                    <a href="/admin/accountreniew/view/@obj.CompanyRegistrationPaymentId" title="View" class="btn btn-icon btn-success"><i class="fas fa-eye"></i></a>
                                                </td>
                                            </tr>
                                            counter++;
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

